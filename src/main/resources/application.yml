spring:
  application:
    name: discodeit
  datasource:
    url: jdbc:postgresql://localhost:5432/discodeit # DB?? ?? ??
    username: discodeit_user
    password: discodeit1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create # ?? ??? update, ??? none ??
    show-sql: true       # ?? ?? ??
    properties:
      hibernate:
        format_sql: true # SQL ??? ???
logging:
  level:
    org.hibernate.jdbc.bind: trace
    org.hibernate.SQL: info

discodeit:
  storage:
    type: local
    local:
      root-path: ./uploads
